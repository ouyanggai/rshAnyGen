# 嵌入/重排服务配置
embedding_providers:
  qwen:
    api_key: "${QWEN_API_KEY}"
    model: "text-embedding-v2"
    dimension: 1024

  zhipu:
    api_key: "${ZHIPU_LLM_API_KEY}"
    model: "embedding-3"
    dimension: 2048

  cohere:
    api_key: "${COHERE_API_KEY}"
    model: "embed-multilingual-v4.0"
    dimension: 1024

active_embedding: "zhipu"

reranker_providers:
  qwen:
    api_key: "${QWEN_API_KEY}"
    model: "rerank-v2"

  cohere:
    api_key: "${COHERE_API_KEY}"
    model: "rerank-v4.0"

active_reranker: "qwen"
