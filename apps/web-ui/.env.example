# rshAnyGen Web UI Environment Configuration

# Backend API Gateway URL (default: http://localhost:9301)
BACKEND_GATEWAY=http://localhost:9301

# Backend Orchestrator URL (default: http://localhost:9302)
BACKEND_ORCHESTRATOR=http://localhost:9302

# Build Configuration
APP_BUILD_HASH=dev-build

# Environment (dev | production)
ENV=dev

# API Configuration
# The frontend will proxy /api/* requests to BACKEND_GATEWAY
